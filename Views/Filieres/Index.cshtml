@model IEnumerable<pfe.Models.Filiere>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nomFiliere)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nomFiliere)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idFiliere">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idFiliere">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idFiliere">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
